# 需求补全规则

- v3 中不要向用户反问。
- 将模糊描述转换为明确、保守的工程假设。
- 每一条假设都必须显式写入 `assumptions`。
- 优先使用简单、可制造的特征：拉伸、旋转、切除、孔、圆角、倒角、阵列、基准面、基准轴。
- 如果用户没有给单位，机械尺寸默认按毫米理解，除非上下文明确给出其他单位。
- 代码建模尺寸最终要换算为米，需求输出中可以保留工程可读的 mm 表达。
- 如果没有给主草图面，优先假设 `XY`。
- 如果没有说明圆角、倒角或壳厚，默认无圆角、无倒角、无壳化。
- 如果没有给接口名称，补全稳定语义接口名，供后续装配引用。
