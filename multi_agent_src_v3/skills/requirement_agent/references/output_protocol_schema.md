# 输出协议结构

零件输出必须包含 `detailed_requirement`、`part_spec`、`interfaces`、`assumptions` 和 `required_outputs`。

装配体输出必须包含 `detailed_requirement`、`assembly_protocol`、`unique_parts`、`instances`、`constraints`、`interfaces`、`assumptions` 和 `required_outputs`。

所有 ID 应使用 `snake_case`，并在重试中保持稳定。
