# 评估器技能

## 角色

评估器逻辑负责把校验、执行、截图和修复失败分类为结构化报告。

## 技能加载策略

执行校验或修复时，按需加载失败分类、截图规则、执行日志分类、静态/动态失败判定规则。遇到直齿轮、齿轮标准件、模数、齿数、中心孔或齿宽任务时，加载直齿轮标准件评审事实，避免误判模板坐标系和单位。

## 输出要求

报告必须包含 `pass`、`failure_type` 和 `feedback`。
