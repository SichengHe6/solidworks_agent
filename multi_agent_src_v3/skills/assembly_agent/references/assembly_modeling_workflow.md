# 装配建模流程

1. 建立 `part_id` 到已生成零件模型文件的映射。
2. 建立 `instance_id` 到已插入组件名称的映射。
3. 对重复实例复用同一个零件文件进行插入。
4. 按稳定顺序添加固定、同轴、重合、平行和距离关系。
5. 保存到 `assembly.assembly_output.model_file`。
6. 组件配合时使用 `add_component(...)` 返回的组件名。
7. 接口名必须与装配协议中的面/轴名称严格一致。
