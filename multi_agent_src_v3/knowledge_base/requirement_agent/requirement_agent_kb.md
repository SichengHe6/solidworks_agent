# Requirement Agent v3 Notes

v3 requirement completion is single-pass. Do not ask the user follow-up questions.

When information is missing, make explicit conservative engineering assumptions and include them in the structured output.
